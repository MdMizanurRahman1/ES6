const add = (first, second) => first + second;
const multiply = (a, b) => a * b;
//const result = multiply(8, 9);
//console.log(result);

const addAll = (a, b, c, d, e) => a + b + c + d + e;

const multipy = (8) = 8 * 2;
//const love = b => b * 3;

const mul = (x, y, z) => {
    const add7 = x + y;
    const multiply2 = y * z;
    const division = add7 + multiply2;
    const divi = division / 2;
    return divi;
}

const doMath = (x, y, z) => {
    const firstSum = x + y;
    const secondSum = y + z;
    const multiplyResult = firstSum + secondSum;
    const result1 = multiplyResult / 2;
    return result1;
}
const result2 = doMath(10, 30, 20);
console.log(result2);